# Dataset download (173 manually annotated manuscript images + YOLO-format labels):
# https://drive.google.com/drive/folders/1wDzXJT2WOGMleG9ejMIIcVL01J5QW4Pd
#
# Download and extract the folder, then place it as a sibling of `training/`
# named `dataset/` (matching the path below), or edit `path:` to point at
# wherever you extracted it.

path: ../dataset

train: images/train
val: images/val

names:
  0: main_text
  1: header_text
  2: footer_text
  3: side_text
  4: filler